# Copie ce fichier en ".env" et remplace la valeur par ta vraie cle.
# Le vrai fichier .env est ignore par Git (voir .gitignore) : il ne sera
# jamais commit, donc ta cle reste privee.

VOYAGE_API_KEY=ta_cle_voyage_ici
